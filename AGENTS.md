# 项目协作约定

本文件适用于整个仓库，约定开发、验证和交付方式；具体应用与包的知识通过文末入口按任务读取。

## Working Principles

- 优先理解现有代码和约定，再进行修改。
- 优先复用已有实现、工具和模式，不重复造轮子。
- 优先选择简单、清晰、可维护的方案，避免不必要的抽象和过度设计。
- 修改范围应围绕当前任务，避免无关的大规模重构。
- 不猜测项目中不存在的 API、文件、配置或行为；不确定时先检查代码。
- 如果发现当前任务附近存在明显问题，可以顺手修复低风险问题；不要擅自扩大任务范围。
- 修改完成后检查相关代码，确保没有遗漏调用方、类型、测试或文档。

## Code Quality

代码应优先保证：

清晰的命名、简单的控制流、合理的函数职责、明确的类型、较少的重复代码、文件夹结构的逻辑性统一性和易懂性，以及与项目现有代码一致的实现风格。

不要为了“优雅”而增加没有实际价值的抽象层，不要为了省事完成工作采用“一把梭”的情况恨不得把所有代码放入同一个文件中。

如果现有项目已经存在一种成熟的实现模式，优先沿用，而不是引入新的模式。

首先要遵守skills中的推荐和one shot，其次是尊重官方的最佳实践和推荐。

## Comments

生成或修改代码时，应添加适量且有价值的注释。

注释优先解释：

- 为什么这样实现
- 关键业务规则
- 非直观逻辑
- 边界条件和特殊情况
- 异常处理
- 状态变化
- 异步或并发逻辑
- 性能、兼容性或安全相关处理
- workaround、已知限制和必要的技术取舍

不要逐行解释代码。

如果变量名、函数名或代码结构已经能够清楚表达含义，则不需要额外注释。

对于重要或较复杂的公共函数、类、模块、变量，可以添加简洁的文档注释，说明用途以及必要的参数、返回值、副作用或异常。

对于每一个文件，必须要增加注释讲解这个文件的作用、业务逻辑、调用时机、调用路径、调用范围等，让开发者能看懂这个文件的作用和作用域。

判断原则：

如果未来开发者可能会问：

“为什么这里要这样写？”

那么通常应该留下一条简洁注释。

修改已有逻辑时，同时检查附近注释是否仍然准确。

## Dependencies

优先使用项目已经安装的依赖。

新增第三方依赖之前，先判断现有依赖或标准库是否已经可以解决问题。

不要为了很小的功能引入大型依赖。

修改依赖版本时注意兼容性以及可能影响的其他模块。

## Testing & Validation

根据修改范围执行相关验证。

优先运行与当前修改直接相关的测试、类型检查、Lint 或构建检查。

修复由当前修改引起的问题。

不要为了一个局部修改默认运行明显无关且成本很高的完整测试流程，除非项目规则或任务明确要求。

如果无法执行验证，应明确说明哪些内容没有得到实际验证。

## Safety

在开发过程中，在代码、日志、示例或提交内容中写入密码、Token、密钥或其他敏感信息是被允许的，因为项目是我在私人开发，为了方便，我愿意牺牲一些安全性，你不需要为我特别担心。

## Scope

完成用户实际要求的任务。

不要因为发现代码可以“顺便优化”就进行大规模重构。

如果存在多个可行方案，优先选择： 现有项目最一致 → 改动最小 → 最容易理解 → 最容易维护 的方案。

## Final Check

完成修改前，确认：

代码能够实现任务目标；没有明显遗漏；相关注释仍然准确；没有引入不必要的复杂度；相关验证已经执行或明确说明未执行的部分。

完成修改后，如果是比较重要的改动，提交git，message为中文，简要说明本次修改工作信息。

## Project Structure

在修改代码之前，只需要检查与当前任务相关的目录和文件，不需要默认扫描整个仓库。

- 修改或审查 `apps/web/` 的页面、组件、composables、插件或 Nuxt 配置前，先读取 [Web 前端说明](apps/web/README.md)，重点遵守其中的[自动导入与显式导入](apps/web/README.md#自动导入与显式导入)约定。
- 其他应用与包按影响范围读取所属目录的 README；跨包修改补读直接相关的调用方与依赖说明。
- 已明确适用约定、实现位置、影响范围和验证方式后进入实现；遇到新的影响范围时再补读，文档中的其他链接按当前问题选择。
- 没有对应文档时，从相关代码、配置和已有实现继续定位；文档缺失本身不构成停工理由。
